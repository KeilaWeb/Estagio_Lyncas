export class Item {
    constructor({id, descricaoItem, precoUnitario, quantidade, valorTotal}) {
        this.id = id,
        this.descricaoItem = descricaoItem,
        this.precoUnitario = precoUnitario,
        this.quantidade = quantidade,
        this.valorTotal = valorTotal
    }
}